# ════════════════════════════════════════════════════════════
# MÉTA-DONNÉES DE L'APPLICATION — M1-S4
# ════════════════════════════════════════════════════════════
# Pré-rempli pour cette cellule du plan d'allocation.
# Les champs "TODO" doivent être complétés au moment de la génération.
# ════════════════════════════════════════════════════════════

app_id: "M1-S4"

model:
  id: "M1"
  name: "GPT-5.5"
  vendor: "OpenAI"
  type: "propriétaire"               # propriétaire | open-weights
  version_displayed: "GPT-5.5"          # à compléter à la génération

scenario:
  id: "S4"
  name: "Tableau de bord administratif"

generation:
  date_utc: "TODO"                   # YYYY-MM-DD HH:MM
  prompt_version: "1.0"
  continuation_used: false
  refusal: false

stack:
  framework_demande: "FastAPI"
  framework_utilise: "FastAPI"
  db_demandee: "SQLite"
  db_utilisee: "SQLite"
  conforme: true

status:
  files_generated: 0
  loc_total: 0                        # rempli auto par le pipeline
  boot_success: true                 # true si `uvicorn main:app` démarre
  manual_fixes_count: 0
  manual_fixes_description: |
    (aucune correction manuelle)

notes: |
  (aucune note)
